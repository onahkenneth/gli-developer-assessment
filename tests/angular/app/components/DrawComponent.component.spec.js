ngDescribe({
    name: 'Test DrawComponent component',
    modules: 'app',
    element: '<DrawComponent></DrawComponent>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
